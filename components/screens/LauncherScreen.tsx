"use client";

/**
 * The launcher is empty on purpose — the menu-bar logo tooltip/pulse is
 * what the user interacts with. This component exists to keep the screen
 * registry uniform.
 */
export function LauncherScreen() {
  return null;
}

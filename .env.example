# Copy this file to `.env` and fill in the values.
# `.env` is gitignored; `.env.example` should be committed.
#
# Pattern: KEY=value
#   - Keep keys in SCREAMING_SNAKE_CASE.
#   - Quote values that contain spaces or special characters, e.g. KEY="my value".
#   - Do not commit real secrets to this file.

EXAMPLE_VARIABLE=
